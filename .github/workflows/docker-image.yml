name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: /docker/puneethreddyhc/agroap
    - name: agroap
      run: docker build . --file Dockerfile --tag agroapp:$(date +%s)
